# CLAUDE.md

> 完整指南位于 `.claude/CLAUDE.md`，请优先阅读该文件获取项目架构、约定与陷阱。
> 记忆索引位于 `.claude/MEMORY.md`，记忆文件位于 `.claude/memory/`。

本项目是 **LingServer Dashboard v2.0** — 单页服务器运维面板（FastAPI + SQLite + Vanilla JS）。

启动：`./start.sh --dev`  测试：`./test.sh`  地址：`http://localhost:8899`

开始工作前务必阅读 `.claude/CLAUDE.md` 了解完整上下文。
